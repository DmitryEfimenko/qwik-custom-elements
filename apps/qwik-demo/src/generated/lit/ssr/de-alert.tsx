// Generated by @qwik-custom-elements/core. Project: demo-lit-ssr.
// Do not edit this file directly. Use a manual extension layer.

import { Slot, component$ } from '@builder.io/qwik';
import { TestLitLibSSRBridgeComponent } from './runtime';

export interface QwikDeAlertProps {
  heading?: string;
  [key: string]: unknown;
}

export const QwikDeAlert = component$<QwikDeAlertProps>((props) => {
  const isEventBindingKey = (key: string) =>
    /^on[A-Z].*\$$/.test(key) || key.includes(':');
  const eventProps: Record<string, unknown> = {};
  const elementProps: Record<string, unknown> = {};

  for (const [key, value] of Object.entries(props as Record<string, unknown>)) {
    if (key === 'children') {
      continue;
    }

    if (isEventBindingKey(key)) {
      eventProps[key] = value;
      continue;
    }

    elementProps[key] = value;
  }

  const passthroughEventProps = eventProps;
  const mappedEvents = undefined;

  return (
    <TestLitLibSSRBridgeComponent
      tagName="de-alert"
      props={elementProps}
      events={mappedEvents}
      slots={undefined}
      {...passthroughEventProps}
    >
    <Slot />
    </TestLitLibSSRBridgeComponent>
  );
});
